# Operating Entry

1. Read this file, `CURRENT.md`, and `DECISIONS.md` before complex work.
2. Use only the installed `pkc` CLI; never access `.local` or SQLite directly.
3. Run bounded L1/L2 retrieval before L3.
4. Use `knowledge-plan` for production Claim and Authority Reference changes.
5. Stop for explicit approval of the exact immutable Bundle hash before apply.
6. Do not mutate Git unless the project owner explicitly requests it.
