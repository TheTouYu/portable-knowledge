# Current Recovery

Primary Context: `default`

Goal: verify the neutral PKC installation and replace this example with project-owned facts only after explicit review.

Next action: run `pkc validate`, `pkc rebuild`, and one representative query.
