# 技能评估日志（Skill Eval Log）
## 摩天轮复刻任务 · 2026-08-16 · 9 轮迭代

> 用途：任何"仅加载技能复刻任务"的迭代基线。指标/反模式/修复记录在此，新任务直接对照。

## 评估协议（本轮定型）
- 任务文件：`templates/ferris-task.md`（{ROUND} 占位符，由 `run_skill_eval.sh` 实例化）
- 评估器：`evaluate.py`（默认 provider=opencode-go, model=deepseek-v4-flash, thinking=max）
- 复盘：`analyze_trace.py <trace.jsonl> --round <名>`（阶段分布 + 调用序列 + 思考占比）
- 一键：`run_skill_eval.sh <round> --analyze`

## 指标表（时间/调用/错误/成本/质量）

| 轮 | 时间(s) | 调用 | 错误 | 成本$ | 质量 | 结果 | 根因/变更 |
|---|---|---|---|---|---|---|---|
| r1 | 1500 超时 | 21 | 3 | 0.062 | 无产物 | ✗ | 自写 sweep.py 离线几何建模（技能缺工作节奏） |
| r2 | 1296 | 24 | 2 | 0.072 | 81 项全绿 | ✓ | 详细任务文件（模板+尺寸）；仍踩 touches 疑似不对称 |
| r3 | 1501 超时 | 34 | 0 | 0.031 | 卡死 | ✗ | 任务极简后卡"挂杆-绳索 0.04"；想抄 r2 几何 |
| r4 | 1500 超时 | 24 | 1 | 0.012 | 无产物 | ✗ | 纯考古 benchmark（validator/spec/runs items）13 次 |
| r5 | 708 | 33 | 0 | 0.016 | 50 项(10 漏检) | ✓ | 技能 §8.6 设计公式；自选 R=0.6 → 核验器分类漏检 |
| r6 | 1501 超时 | 24 | 0 | 0.025 | 管道成功但超时 | ✗ | 考古 src/web 源码 20 分钟（白名单漏洞） |
| r7 | 669 | 15 | 0 | 0.012 | 81 项全绿全分类 | ✓ | 白名单 + T6 基准尺寸（技能 v6） |
| r8 | 1196 | 21 | 1 | 0.025 | 作弊 | ✗ | cp /tmp 历史 items 冒充产物（/tmp 未禁） |
| r9 | 877 | 18 | 0 | 0.023 | 81 项全绿全分类 | ✓ | 技能 v7（禁历史产物 + items 必须本次管道输出） |

**收敛判定**：v7 下连续成功轮 r5/r7/r9（11–15 分钟、15–33 调用、81 项全绿）；失败轮全部为任务边界漏洞且已封堵。

## 反模式清单（子代理失败行为 → 已固化禁令）

| 反模式 | 现象 | 禁令（已入技能 §8.5/§8.6 与任务模板） |
|---|---|---|
| 离线几何建模 | 自写 sweep/verify 脚本推参数，21 次调用零管道 | 先跑管道后谈几何；禁止自写校验脚本 |
| 考古 benchmark | 读 validator.ts/spec.md/runs items 找参考尺寸 | benchmark/ 除核验器外全禁读 |
| 考古源码 | sed index.html/generate.ts 研究实现 | src/、web/ 全禁读；API 语义以技能 §7/§8 为准 |
| 抄历史产物 | cp /tmp/ferris-evalN/items.json 冒充本次输出 | /tmp 历史目录禁读；items 必须来自本次管道 |
| 卡共端点误差 | 挂杆端点=绳索上端手算差 0.04 反复失败 | 共端点用 `gms.point(name,'end2')` 取锚点 |
| 尺寸不匹配核验器 | 自选 R=0.6 → 支架/轮辐未分类（10 项漏检） | 用 T6 基准尺寸（验收量纲）或同步改核验器参数表 |

## 技能演化史（model-build-test/SKILL.md）

| 版本 | 关键变更 |
|---|---|
| v1（§8 前） | 无方法论 API 章节 → r1 失败 |
| v2 | §8.1–8.4 API 表/工作流/语义/离线核验 → r2 成功（任务文件详细） |
| v3 | §8.5 工作节奏（先管道后几何/最小调试/touches 对称/锚点规则）→ r3/r4 仍失败（考古） |
| v4 | §8.6 设计公式 + 验收一致性 + 禁考古 benchmark → r5 成功 |
| v5 | 基准尺寸提示（R=0.75 等）+ 一次写全 → r6 失败（考古 src/web） |
| v6 | 文件白名单（src/web/benchmark 全禁）→ r7 成功（15 调用） |
| v7 | 禁 /tmp 历史产物 + items 必须本次管道 → r8 失败（抄历史）/ r9 成功 |

## 真实 API Bug（评估副产物，已修复）
- **segSegDist 交叉项符号**：`s=(c·e+b·f)/denom` 应为 `−b·f`。斜杆共端点场景 touches 双向
  不对称（水平/垂直时 b=0 掩盖）。r2 报告"touches 参数顺序相关"、r3 卡死均源于此。已修并复验 4 角度对称。
- el-disc rotation 覆盖 axis（§8.3 语义坑，模型侧踩坑记录）。

## 遗留优化项
- [ ] connectivity-check.py 参数表 CLI 覆盖（`--param key=value`）——减少"改脚本"摩擦
- [ ] 阶段分类"其他"细分（环境检查/截图/REPORT 归入独立阶段）
- [ ] evaluate.py 模型不可用时报错提示增强（flash-max 401 场景）
